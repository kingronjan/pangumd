# Invoked via: /git:sync

description = "提交并推送缓冲区文件（如果缓冲区为空，则包含所有变更文件）"
prompt = """

请帮我执行完整的 Git 提交和推送流程：
    1. 检查暂存区，如果为空，则添加所有变更的文件；
    2. 阅读变更并生成提交信息；
    3. 使用提交信息提交，如果提交失败（文件被 git hooks 修改），则再次添加修改后的文件，再次提交；
    4. 推送到远程仓库的当前分支。

"""
